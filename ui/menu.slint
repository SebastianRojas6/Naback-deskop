import {Background} from "background.slint";

export component Menu inherits Background {
        Text {
             text: "¡Elige uno!";
             color: #37383F;
             font-size: 70px ;
             font-family: "Minecraft";
             height: parent.height * 0.90;
}

}